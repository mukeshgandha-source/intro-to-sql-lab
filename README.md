# 🕵️‍♂️ Intro to SQL Lab: The Hunt for Carmen Sandiego

This repository contains my solutions for the **Intro to SQL Lab**. The goal of this project is to use SQL queries to track down the world-famous thief, Carmen Sandiego, by navigating through a database of countries, cities, and languages.

## 🚀 Project Overview
In this lab, I practiced:
* Creating and managing a PostgreSQL database.
* Importing large SQL datasets using the `\i` command.
* Writing complex `SELECT` queries with `WHERE` clauses.
* Joining multiple tables (`city`, `country`, `countrylanguage`) to find specific information.

## 🛠 Setup Instructions
To run this project locally:

1. **Install PostgreSQL**: Ensure you have PostgreSQL installed on your machine.
2. **Create Database**:
   ```sql
   CREATE DATABASE world;
